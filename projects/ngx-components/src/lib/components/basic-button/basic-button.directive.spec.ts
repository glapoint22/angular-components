import { BasicButtonDirective } from './basic-button.directive';

describe('ButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new BasicButtonDirective();
    expect(directive).toBeTruthy();
  });
});
